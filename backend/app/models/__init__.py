"""Domain models package.

Database-facing domain models (organization, document, research run,
report, evaluation) are introduced together with their persistence
layers in later phases. API request/response schemas live in
``app/api/schemas.py`` (versioned API contracts).
"""
